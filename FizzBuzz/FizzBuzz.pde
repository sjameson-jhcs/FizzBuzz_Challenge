int current_number = 0;
void setup(){
  
}

void draw(){
  int last_number = 100;
  current_number++;
  //The currently prints the numbers from 1 to 100
  //Change the code so that any number divisible by 3 prints "Fizz" instead of the number
  //Similarly, print "Buzz" instead if it divisible by 5
  //Lastly replace numbers divisible by both with "FizzBuzz"
  println(current_number);
}
